# Backend origin that Vite proxies /api requests to during development.
VITE_API_PROXY=http://localhost:5080

# Optional: bypass the dev proxy and call the API directly (must include /api).
# VITE_API_BASE_URL=http://localhost:5080/api
